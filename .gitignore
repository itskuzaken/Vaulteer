# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# Ignore node_modules in the repository root and in package subfolders (monorepo)
node_modules/
/frontend/node_modules/
/backend/node_modules/
/node_modules/@next
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# frontend next.js
/frontend/.next

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

todo.txt
vaulteer.sql
vaulteer-structure.txt
vaulteer_db.sql

# Backup files
*.bak
*.backup
*.tmp

# Ignore markdown files in root directory only
/*.md
backend/schema.sql
firebase-service-account.json
backend/firebase-service-account.json

# project documents (ignored)
/document/
docs
AWS_DEPLOYMENT_INSTRUCTIONS.md
# HYBRID_API_SECURITY.md - Keep this for documentation
# .gitignore - Don't ignore this file itself

# Security setup scripts (keep)
!setup-security.sh
!setup-security.bat

# Ignore log files
/backend/logs/*.json

/.github/instructions/*

# Ignore SQL migrations and script files (generated / environment-specific)
/backend/migrations/
/backend/migrations/**/*.sql
/backend/migrations/**/*.js
/scripts/
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab/backend/scripts/

# Ignore migration-runner artifacts
/backend/migrate.js

# Ignore IDE config files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
# Ignore OS generated files
Thumbs.db
Desktop.ini

# Ignore test output files
/backend/__tests__/output/
/backend/__tests__/output/*
!/backend/__tests__/output/.gitkeep
/backend/__tests__/temp/
/backend/__tests__/temp/*
!/backend/__tests__/temp/.gitkeep
/frontend/src/__tests__/*

.github/prompts/*

backend/# Join Waitlist Feature Plan.prompt.md
backend/__tests__/*
backend/scripts/*